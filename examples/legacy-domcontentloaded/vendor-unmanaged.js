/**
 * Byte for byte the same pattern as vendor-dcl.js. The only difference is
 * that this file is injected by hand instead of through QSL.
 */
document.addEventListener('DOMContentLoaded', function () {
  window.vendorBooted('unmanaged');
});
