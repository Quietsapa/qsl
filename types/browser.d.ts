/**
 * The browser bundles (`@quietsapa/qsl/browser`, `@quietsapa/qsl/browser/slim`)
 * export nothing: they register their types, start QSL and leave the
 * instance on `window.__QSL__`, typed by the global declarations below.
 */
import type {} from './index.js';

export {};
